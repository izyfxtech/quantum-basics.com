import { t as supabase } from "./client-CFL8hqg1.js";
import { c as createEvent, g as PortalShell, l as fetchCourses, m as postNotice, n as fetchMyAccess, o as roleBadges, t as ROLE_LABELS } from "./roles-X33_OEsl.js";
import { c as TextField, r as Panel, t as Btn } from "./ui-pvzeBS6D.js";
import { a as assignCourseStaff, c as isAssignablePlatformRole, i as removeCourseStaffMember, l as removePlatformRole, o as assignPlatformRole, s as fetchPlatformStats, u as searchProfiles } from "./staff-Bnu4PpAQ.js";
import { useCallback, useEffect, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { Loader2, Search, UserPlus, X } from "lucide-react";
//#region src/routes/academy._authenticated.admin.tsx?tsr-split=component
var PLATFORM_ROLE_OPTIONS = [
	"super_admin",
	"auditor",
	"student"
];
var COURSE_ROLE_OPTIONS = ["instructor", "teaching_assistant"];
function AdminPanel() {
	const [name, setName] = useState("Admin");
	const [badges, setBadges] = useState([]);
	const [stats, setStats] = useState(null);
	const [courses, setCourses] = useState([]);
	const [query, setQuery] = useState("");
	const [results, setResults] = useState(null);
	const [searching, setSearching] = useState(false);
	const search = useCallback(async (q) => {
		setSearching(true);
		setResults(await searchProfiles(q));
		setSearching(false);
	}, []);
	const loadShell = useCallback(async () => {
		const [{ data: userData }, access, platformStats, courseList] = await Promise.all([
			supabase.auth.getUser(),
			fetchMyAccess(),
			fetchPlatformStats(),
			fetchCourses()
		]);
		if (userData.user) {
			const { data: profile } = await supabase.from("profiles").select("full_name").eq("id", userData.user.id).maybeSingle();
			setName(profile?.full_name?.trim() || userData.user.email || "Admin");
		}
		setBadges(roleBadges(access));
		setStats(platformStats);
		setCourses(courseList);
	}, []);
	useEffect(() => {
		loadShell();
		search("");
	}, [loadShell, search]);
	return /* @__PURE__ */ jsxs(PortalShell, {
		name,
		roles: badges,
		showAdmin: true,
		children: [
			/* @__PURE__ */ jsxs("div", { children: [
				/* @__PURE__ */ jsx("p", {
					className: "qa-label text-primary",
					children: "Academy admin"
				}),
				/* @__PURE__ */ jsx("h1", {
					className: "mt-3 text-2xl font-semibold tracking-tight md:text-3xl",
					children: "Roles & staffing"
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-2 max-w-xl text-sm text-muted-foreground",
					children: "Assign platform roles and staff courses with instructors and teaching assistants."
				})
			] }),
			stats ? /* @__PURE__ */ jsxs("div", {
				className: "mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-5",
				children: [
					/* @__PURE__ */ jsx(StatCard, {
						label: "Users",
						value: stats.totalUsers
					}),
					/* @__PURE__ */ jsx(StatCard, {
						label: "Courses",
						value: stats.totalCourses
					}),
					/* @__PURE__ */ jsx(StatCard, {
						label: "Enrolments",
						value: stats.totalEnrollments
					}),
					/* @__PURE__ */ jsx(StatCard, {
						label: "Instructors",
						value: stats.totalInstructors
					}),
					/* @__PURE__ */ jsx(StatCard, {
						label: "Teaching assistants",
						value: stats.totalTAs
					})
				]
			}) : null,
			/* @__PURE__ */ jsxs("section", {
				className: "mt-12 grid gap-4 lg:grid-cols-2",
				children: [/* @__PURE__ */ jsx(NoticeForm, { courses }), /* @__PURE__ */ jsx(EventForm, { courses })]
			}),
			/* @__PURE__ */ jsxs("section", {
				className: "mt-12",
				children: [
					/* @__PURE__ */ jsx("h2", {
						className: "text-lg font-semibold tracking-tight",
						children: "Find a user"
					}),
					/* @__PURE__ */ jsxs("form", {
						onSubmit: (e) => {
							e.preventDefault();
							search(query);
						},
						className: "mt-4 flex gap-2",
						children: [/* @__PURE__ */ jsxs("div", {
							className: "relative flex-1",
							children: [/* @__PURE__ */ jsx(Search, { className: "pointer-events-none absolute left-3.5 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground" }), /* @__PURE__ */ jsx("input", {
								value: query,
								onChange: (e) => setQuery(e.target.value),
								placeholder: "Search by name or email…",
								className: "h-11 w-full rounded-md border border-input bg-background pl-10 pr-3 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-ring/30"
							})]
						}), /* @__PURE__ */ jsx("button", {
							type: "submit",
							className: "inline-flex h-11 items-center justify-center rounded-full bg-primary px-5 text-sm font-semibold text-primary-foreground transition-colors hover:bg-primary/90",
							children: "Search"
						})]
					}),
					/* @__PURE__ */ jsx("div", {
						className: "mt-6 space-y-4",
						children: searching ? /* @__PURE__ */ jsxs("p", {
							className: "flex items-center gap-2 text-sm text-muted-foreground",
							children: [/* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin" }), " Searching…"]
						}) : results && results.length === 0 ? /* @__PURE__ */ jsx("p", {
							className: "text-sm text-muted-foreground",
							children: "No users found."
						}) : results?.map((row) => /* @__PURE__ */ jsx(AdminUserRow, {
							row,
							courses,
							onChanged: () => search(query)
						}, row.id))
					})
				]
			})
		]
	});
}
function CourseSelect({ courses, value, onChange }) {
	return /* @__PURE__ */ jsxs("label", {
		className: "block",
		children: [/* @__PURE__ */ jsx("span", {
			className: "qa-label text-muted-foreground",
			children: "Scope"
		}), /* @__PURE__ */ jsxs("select", {
			value,
			onChange: (e) => onChange(e.target.value),
			className: "mt-1.5 h-11 w-full rounded border border-[var(--qa-line)] bg-card px-3 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-ring/25",
			children: [/* @__PURE__ */ jsx("option", {
				value: "",
				children: "Academy-wide"
			}), courses.map((c) => /* @__PURE__ */ jsx("option", {
				value: c.id,
				children: c.title
			}, c.id))]
		})]
	});
}
function NoticeForm({ courses }) {
	const [title, setTitle] = useState("");
	const [body, setBody] = useState("");
	const [courseId, setCourseId] = useState("");
	const [busy, setBusy] = useState(false);
	const [status, setStatus] = useState("idle");
	async function handleSubmit(e) {
		e.preventDefault();
		if (!title.trim() || !body.trim()) return;
		setBusy(true);
		const { error } = await postNotice({
			title: title.trim(),
			body: body.trim(),
			courseId: courseId || null
		});
		setBusy(false);
		if (error) {
			setStatus("error");
			return;
		}
		setStatus("posted");
		setTitle("");
		setBody("");
		setCourseId("");
	}
	return /* @__PURE__ */ jsxs(Panel, { children: [/* @__PURE__ */ jsx("h2", {
		className: "text-base font-semibold",
		children: "Post a notice"
	}), /* @__PURE__ */ jsxs("form", {
		onSubmit: handleSubmit,
		className: "mt-4 grid gap-4",
		children: [
			/* @__PURE__ */ jsx(TextField, {
				label: "Title",
				value: title,
				onChange: setTitle,
				placeholder: "New module released",
				required: true
			}),
			/* @__PURE__ */ jsxs("label", {
				className: "block",
				children: [/* @__PURE__ */ jsx("span", {
					className: "qa-label text-muted-foreground",
					children: "Body"
				}), /* @__PURE__ */ jsx("textarea", {
					value: body,
					onChange: (e) => setBody(e.target.value),
					required: true,
					rows: 3,
					className: "mt-1.5 w-full rounded border border-[var(--qa-line)] bg-card px-3 py-2 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-ring/25"
				})]
			}),
			/* @__PURE__ */ jsx(CourseSelect, {
				courses,
				value: courseId,
				onChange: setCourseId
			}),
			/* @__PURE__ */ jsx(Btn, {
				type: "submit",
				disabled: busy,
				children: busy ? "Posting…" : "Post notice"
			}),
			status === "posted" ? /* @__PURE__ */ jsx("p", {
				className: "text-sm text-primary",
				children: "Notice posted."
			}) : null,
			status === "error" ? /* @__PURE__ */ jsx("p", {
				className: "text-sm text-destructive",
				children: "Couldn't post that notice. Try again."
			}) : null
		]
	})] });
}
function EventForm({ courses }) {
	const [title, setTitle] = useState("");
	const [description, setDescription] = useState("");
	const [date, setDate] = useState("");
	const [courseId, setCourseId] = useState("");
	const [busy, setBusy] = useState(false);
	const [status, setStatus] = useState("idle");
	async function handleSubmit(e) {
		e.preventDefault();
		if (!title.trim() || !date) return;
		setBusy(true);
		const { error } = await createEvent({
			title: title.trim(),
			description: description.trim(),
			date,
			courseId: courseId || null
		});
		setBusy(false);
		if (error) {
			setStatus("error");
			return;
		}
		setStatus("posted");
		setTitle("");
		setDescription("");
		setDate("");
		setCourseId("");
	}
	return /* @__PURE__ */ jsxs(Panel, { children: [/* @__PURE__ */ jsx("h2", {
		className: "text-base font-semibold",
		children: "Add a calendar event"
	}), /* @__PURE__ */ jsxs("form", {
		onSubmit: handleSubmit,
		className: "mt-4 grid gap-4",
		children: [
			/* @__PURE__ */ jsx(TextField, {
				label: "Title",
				value: title,
				onChange: setTitle,
				placeholder: "Assessment window opens",
				required: true
			}),
			/* @__PURE__ */ jsx(TextField, {
				label: "Date",
				type: "date",
				value: date,
				onChange: setDate,
				required: true
			}),
			/* @__PURE__ */ jsxs("label", {
				className: "block",
				children: [/* @__PURE__ */ jsx("span", {
					className: "qa-label text-muted-foreground",
					children: "Description (optional)"
				}), /* @__PURE__ */ jsx("textarea", {
					value: description,
					onChange: (e) => setDescription(e.target.value),
					rows: 2,
					className: "mt-1.5 w-full rounded border border-[var(--qa-line)] bg-card px-3 py-2 text-sm outline-none focus:border-primary focus:ring-2 focus:ring-ring/25"
				})]
			}),
			/* @__PURE__ */ jsx(CourseSelect, {
				courses,
				value: courseId,
				onChange: setCourseId
			}),
			/* @__PURE__ */ jsx(Btn, {
				type: "submit",
				disabled: busy,
				children: busy ? "Adding…" : "Add event"
			}),
			status === "posted" ? /* @__PURE__ */ jsx("p", {
				className: "text-sm text-primary",
				children: "Event added."
			}) : null,
			status === "error" ? /* @__PURE__ */ jsx("p", {
				className: "text-sm text-destructive",
				children: "Couldn't add that event. Try again."
			}) : null
		]
	})] });
}
function StatCard({ label, value }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "qa-card p-6",
		children: [/* @__PURE__ */ jsx("p", {
			className: "text-3xl font-semibold tracking-tight",
			children: value
		}), /* @__PURE__ */ jsx("p", {
			className: "mt-1 text-xs uppercase tracking-widest text-muted-foreground",
			children: label
		})]
	});
}
function AdminUserRow({ row, courses, onChanged }) {
	const [busy, setBusy] = useState(false);
	const [newPlatformRole, setNewPlatformRole] = useState("");
	const [newCourseId, setNewCourseId] = useState("");
	const [newCourseRole, setNewCourseRole] = useState("instructor");
	const assignablePlatformRoles = PLATFORM_ROLE_OPTIONS.filter((r) => isAssignablePlatformRole(r) && !row.platformRoles.includes(r));
	async function handleAddPlatformRole() {
		if (!newPlatformRole) return;
		setBusy(true);
		await assignPlatformRole(row.id, newPlatformRole);
		setBusy(false);
		setNewPlatformRole("");
		onChanged();
	}
	async function handleRemovePlatformRole(role) {
		setBusy(true);
		await removePlatformRole(row.id, role);
		setBusy(false);
		onChanged();
	}
	async function handleAddCourseStaff() {
		if (!newCourseId) return;
		setBusy(true);
		await assignCourseStaff(newCourseId, row.id, newCourseRole);
		setBusy(false);
		setNewCourseId("");
		onChanged();
	}
	async function handleRemoveCourseStaff(staffId) {
		setBusy(true);
		await removeCourseStaffMember(staffId);
		setBusy(false);
		onChanged();
	}
	return /* @__PURE__ */ jsxs("div", {
		className: "qa-card p-6",
		children: [
			/* @__PURE__ */ jsx("div", {
				className: "flex flex-wrap items-start justify-between gap-3",
				children: /* @__PURE__ */ jsxs("div", { children: [
					/* @__PURE__ */ jsx("p", {
						className: "font-semibold",
						children: row.fullName || "Unnamed"
					}),
					/* @__PURE__ */ jsx("p", {
						className: "text-sm text-muted-foreground",
						children: row.email
					}),
					row.organisation ? /* @__PURE__ */ jsx("p", {
						className: "text-xs text-muted-foreground",
						children: row.organisation
					}) : null
				] })
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "mt-4 flex flex-wrap gap-1.5",
				children: [row.platformRoles.map((role) => /* @__PURE__ */ jsx(RoleBadge, {
					label: ROLE_LABELS[role],
					onRemove: () => handleRemovePlatformRole(role),
					disabled: busy
				}, role)), row.courseStaff.map((cs) => /* @__PURE__ */ jsx(RoleBadge, {
					label: `${ROLE_LABELS[cs.role]} · ${cs.courseTitle}`,
					onRemove: () => handleRemoveCourseStaff(cs.staffId),
					disabled: busy
				}, cs.staffId))]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "mt-4 flex flex-wrap gap-2",
				children: [
					/* @__PURE__ */ jsxs("select", {
						value: newPlatformRole,
						onChange: (e) => setNewPlatformRole(e.target.value),
						className: "h-9 rounded-md border border-input bg-background px-2 text-xs outline-none focus:border-primary",
						children: [/* @__PURE__ */ jsx("option", {
							value: "",
							children: "Add platform role…"
						}), assignablePlatformRoles.map((role) => /* @__PURE__ */ jsx("option", {
							value: role,
							children: ROLE_LABELS[role]
						}, role))]
					}),
					/* @__PURE__ */ jsxs("button", {
						type: "button",
						disabled: !newPlatformRole || busy,
						onClick: handleAddPlatformRole,
						className: "inline-flex h-9 items-center gap-1 rounded-full bg-secondary px-3 text-xs font-semibold transition-colors hover:bg-secondary/70 disabled:opacity-60",
						children: [/* @__PURE__ */ jsx(UserPlus, { className: "h-3.5 w-3.5" }), " Add"]
					}),
					/* @__PURE__ */ jsxs("select", {
						value: newCourseId,
						onChange: (e) => setNewCourseId(e.target.value),
						className: "h-9 rounded-md border border-input bg-background px-2 text-xs outline-none focus:border-primary",
						children: [/* @__PURE__ */ jsx("option", {
							value: "",
							children: "Assign to course…"
						}), courses.map((c) => /* @__PURE__ */ jsx("option", {
							value: c.id,
							children: c.title
						}, c.id))]
					}),
					/* @__PURE__ */ jsx("select", {
						value: newCourseRole,
						onChange: (e) => setNewCourseRole(e.target.value),
						className: "h-9 rounded-md border border-input bg-background px-2 text-xs outline-none focus:border-primary",
						children: COURSE_ROLE_OPTIONS.map((role) => /* @__PURE__ */ jsx("option", {
							value: role,
							children: ROLE_LABELS[role]
						}, role))
					}),
					/* @__PURE__ */ jsxs("button", {
						type: "button",
						disabled: !newCourseId || busy,
						onClick: handleAddCourseStaff,
						className: "inline-flex h-9 items-center gap-1 rounded-full bg-secondary px-3 text-xs font-semibold transition-colors hover:bg-secondary/70 disabled:opacity-60",
						children: [/* @__PURE__ */ jsx(UserPlus, { className: "h-3.5 w-3.5" }), " Assign"]
					})
				]
			})
		]
	});
}
function RoleBadge({ label, onRemove, disabled }) {
	return /* @__PURE__ */ jsxs("span", {
		className: "inline-flex items-center gap-1.5 rounded-full bg-secondary px-3 py-1 text-xs font-semibold text-secondary-foreground",
		children: [label, /* @__PURE__ */ jsx("button", {
			type: "button",
			disabled,
			onClick: onRemove,
			"aria-label": `Remove ${label}`,
			className: "text-muted-foreground hover:text-destructive disabled:opacity-60",
			children: /* @__PURE__ */ jsx(X, { className: "h-3 w-3" })
		})]
	});
}
//#endregion
export { AdminPanel as component };
