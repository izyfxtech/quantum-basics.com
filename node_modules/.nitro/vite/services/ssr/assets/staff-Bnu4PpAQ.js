import { t as supabase } from "./client-CFL8hqg1.js";
//#region src/academy/lib/admin.ts
/** Platform-wide counts. Visible to super_admin (full) and auditor (via the
* auditor read policies on enrollments/lesson_progress; profiles/course
* counts use head-count queries that don't expose row contents). */
async function fetchPlatformStats() {
	const [users, courses, enrollments, instructors, tas] = await Promise.all([
		supabase.from("profiles").select("id", {
			count: "exact",
			head: true
		}),
		supabase.from("courses").select("id", {
			count: "exact",
			head: true
		}),
		supabase.from("enrollments").select("id", {
			count: "exact",
			head: true
		}),
		supabase.from("course_staff").select("id", {
			count: "exact",
			head: true
		}).eq("role", "instructor"),
		supabase.from("course_staff").select("id", {
			count: "exact",
			head: true
		}).eq("role", "teaching_assistant")
	]);
	return {
		totalUsers: users.count ?? 0,
		totalCourses: courses.count ?? 0,
		totalEnrollments: enrollments.count ?? 0,
		totalInstructors: instructors.count ?? 0,
		totalTAs: tas.count ?? 0
	};
}
/** Search across all profiles by name or email. Only super_admin can see
* rows beyond their own under RLS, so this is effectively admin-only. */
async function searchProfiles(query) {
	const trimmed = query.trim();
	let builder = supabase.from("profiles").select("id, full_name, email, organisation, user_roles(role), course_staff(id, role, courses(id, title))").order("full_name", { ascending: true }).limit(25);
	if (trimmed) builder = builder.or(`full_name.ilike.%${trimmed}%,email.ilike.%${trimmed}%`);
	const { data } = await builder;
	return (data ?? []).map((row) => {
		const roleRows = row.user_roles ?? [];
		const staffRows = row.course_staff ?? [];
		return {
			id: row.id,
			fullName: row.full_name,
			email: row.email,
			organisation: row.organisation,
			platformRoles: roleRows.map((r) => r.role),
			courseStaff: staffRows.filter((r) => r.courses).map((r) => ({
				staffId: r.id,
				courseId: r.courses.id,
				courseTitle: r.courses.title,
				role: r.role
			}))
		};
	});
}
var ASSIGNABLE_PLATFORM_ROLES = [
	"super_admin",
	"auditor",
	"student"
];
function isAssignablePlatformRole(role) {
	return ASSIGNABLE_PLATFORM_ROLES.includes(role);
}
async function assignPlatformRole(userId, role) {
	const { error } = await supabase.from("user_roles").insert({
		user_id: userId,
		role
	});
	return { error };
}
async function removePlatformRole(userId, role) {
	const { error } = await supabase.from("user_roles").delete().eq("user_id", userId).eq("role", role);
	return { error };
}
/** Super-admin-only: assign a course's instructor or TA directly (no
* enrolment prerequisite — that requirement only applies to an instructor
* self-service-adding a TA, see lib/lms-staff.ts). */
async function assignCourseStaff(courseId, userId, role) {
	const { error } = await supabase.from("course_staff").insert({
		course_id: courseId,
		user_id: userId,
		role
	});
	return { error };
}
//#endregion
//#region src/academy/lib/staff.ts
/** Roster + per-student progress for a single course. Visible to that
* course's instructor/TA (or a super admin) under RLS. */
async function fetchCourseRoster(courseId) {
	const [{ data: enrollments }, { data: lessons }] = await Promise.all([supabase.from("enrollments").select("id, user_id, profiles(full_name, email, organisation)").eq("course_id", courseId), supabase.from("lessons").select("id").eq("course_id", courseId)]);
	const lessonIds = (lessons ?? []).map((l) => l.id);
	const totalLessons = lessonIds.length;
	let completedByUser = /* @__PURE__ */ new Map();
	if (lessonIds.length) {
		const { data: progress } = await supabase.from("lesson_progress").select("user_id, lesson_id").in("lesson_id", lessonIds);
		completedByUser = /* @__PURE__ */ new Map();
		for (const row of progress ?? []) completedByUser.set(row.user_id, (completedByUser.get(row.user_id) ?? 0) + 1);
	}
	return (enrollments ?? []).map((row) => {
		const profile = row.profiles;
		const completed = completedByUser.get(row.user_id) ?? 0;
		return {
			enrollmentId: row.id,
			userId: row.user_id,
			fullName: profile?.full_name ?? null,
			email: profile?.email ?? null,
			organisation: profile?.organisation ?? null,
			completedLessons: completed,
			totalLessons,
			percent: totalLessons ? Math.round(completed / totalLessons * 100) : 0
		};
	});
}
/** Instructors + TAs currently staffed on a course. */
async function fetchCourseStaffList(courseId) {
	const { data } = await supabase.from("course_staff").select("id, user_id, role, profiles(full_name, email)").eq("course_id", courseId);
	return (data ?? []).map((row) => {
		const profile = row.profiles;
		return {
			id: row.id,
			userId: row.user_id,
			role: row.role,
			fullName: profile?.full_name ?? null,
			email: profile?.email ?? null
		};
	});
}
/** An instructor promotes one of their currently-enrolled students to TA on
* their own course. RLS restricts this to the course's instructor(s) and
* to the 'teaching_assistant' role — it cannot be used to add instructors. */
async function promoteStudentToTA(courseId, userId) {
	const { error } = await supabase.from("course_staff").insert({
		course_id: courseId,
		user_id: userId,
		role: "teaching_assistant"
	});
	return { error };
}
/** Remove a course_staff row (super admin: any row; instructor: only a TA
* row on a course they instruct — both enforced by RLS). */
async function removeCourseStaffMember(id) {
	const { error } = await supabase.from("course_staff").delete().eq("id", id);
	return { error };
}
//#endregion
export { assignCourseStaff as a, isAssignablePlatformRole as c, removeCourseStaffMember as i, removePlatformRole as l, fetchCourseStaffList as n, assignPlatformRole as o, promoteStudentToTA as r, fetchPlatformStats as s, fetchCourseRoster as t, searchProfiles as u };
